import { WordCounterType } from '@/components/common/TextField';

export const metaTitleWordInfo: WordCounterType = {
  minimumLength: 50,
  maximumLength: 60,
};

export const metaDescriptionInfo: WordCounterType = {
  minimumLength: 50,
  maximumLength: 160,
};
